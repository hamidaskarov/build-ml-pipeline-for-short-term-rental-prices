name: components
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
    - pip:
      - protobuf==4.23.1
      - mlflow
variables:
  PROTOCOL_BUFFERS_PYTHON_IMPLEMENTATION: python
