name: components
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pyyaml
  - hydra-core
  - pip
  - numpy
  - pip:
      - protobuf==4.23.1
      - mlflow
      - wandb==0.10.31
variables:
  PROTOCOL_BUFFERS_PYTHON_IMPLEMENTATION: python
